# Firebase Configuration for server-side usage (Firebase Functions)
FB_API_KEY=
FB_AUTH_DOMAIN=
FB_PROJECT_ID=
FB_STORAGE_BUCKET=
FB_MESSAGING_SENDER_ID=
FB_APP_ID=
FB_MEASUREMENT_ID=
FB_DATABASE_URL=

# Email Configuration
# Gmail SMTP settings
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Admin notification email (optional, defaults to SMTP_USER)
ADMIN_EMAIL=

# Admin encryption key (server-side only)
ADMIN_ENCRYPTION_KEY=
ADMIN_JWT_SECRET=

# Admin setup key (used once during initial setup)
ADMIN_SETUP_KEY=

# Admin user credentials for scripts (for testing only)
ADMIN_USER_EMAIL=
ADMIN_USER_PASSWORD=

# Google OAuth Configuration (server-side)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Business Configuration
# Tax percentage (0.00 = 0%, 0.08 = 8%, etc.)
TAX_PERCENTAGE=
# Delivery fee in Rs.
DELIVERY_FEE=

# Notes:
# - For Gmail, you need to use an "App Password" instead of your regular password
# - Enable 2-factor authentication on your Gmail account
# - Generate an App Password: https://support.google.com/accounts/answer/185833
# - Use the App Password in SMTP_PASS
# - Tax and delivery fees are set to 0 (free) but can be modified here
